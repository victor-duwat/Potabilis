apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus          # UID fixe, référencé par le dashboard (sinon "No data")
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

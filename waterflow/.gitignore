# ── Artefacts régénérés au runtime ───────────────────────────────
# Base MLflow : le conteneur la recrée au démarrage (scripts/register_model.py)
mlflow_water.db
mlflow_artifacts/
mlruns/

# Base applicative SQLite : recréée au démarrage (init_db) ou par scripts/init_db.py
waterflow2.db
*.db-journal

# Sorties de rapport générées localement
dashboard_xgboost.png
feature_importance.csv
classification_report.txt

# ── Python ───────────────────────────────────────────────────────
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.env
venv/
.venv/

# ── Divers ───────────────────────────────────────────────────────
.claude/
.ipynb_checkpoints/
*.pdf
coverage.xml
.coverage

# NB : model_artifacts/ EST versionné (modèle entraîné + scaler nécessaires
#      au démarrage du conteneur et à la reproductibilité). Ne pas l'ignorer.

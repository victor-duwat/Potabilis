# ── Web framework ─────────────────────────────────────────────────────────
flask>=3.0,<4
gunicorn>=22.0
python-dotenv>=1.0

# ── Base de données ────────────────────────────────────────────────────────
sqlalchemy>=2.0,<3

# ── ML / MLflow ───────────────────────────────────────────────────────────
mlflow>=2.12
xgboost>=2.0
joblib>=1.3
numpy>=1.26
scikit-learn>=1.4        # pour le scaler

# ── OCR & Vision ──────────────────────────────────────────────────────────
anthropic>=0.28          # Claude Vision (fallback OCR)
requests>=2.31           # OCR.space HTTP calls
pymupdf>=1.23            # PDF → images (fitz)

# ── Documentation API ─────────────────────────────────────────────────────
flasgger>=0.9.7.1

# ── Tests ─────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
